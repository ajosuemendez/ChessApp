package com.thechessnuts.InputHandlerService.models;

import com.thechessnuts.InputHandlerService.models.Chess3Board;
import com.thechessnuts.InputHandlerService.models.Square;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SquareTest {
    // Chess3Board board;
    // Square square;
    // String label;
    // int x,y;


    // @Test
    // void invalidPosTest() {
    //     this.board=new Chess3Board();
    //     this.label="g4";
    //     this.x=8;
    //     this.y=2;

    //     assertEquals(-1,new Square(label,x,y,board.sections[2]).getPozX());
    //     assertEquals(-1,new Square(label,x,y,board.sections[2]).getPozY());
    //     this.y=2;
    //     assertEquals(-1,new Square(label,x,y,board.sections[2]).getPozX());
    //     assertEquals(-1,new Square(label,x,y,board.sections[2]).getPozY());

    // }
    // @Test
    // void validPosTest(){
    //     this.board=new Chess3Board();
    //     this.label="g4";
    //     this.x=2;
    //     this.y=2;

    //     assertEquals(x,new Square(label,x,y,board.sections[1]).getPozX());
    //     assertEquals(y,new Square(label,x,y,board.sections[1]).getPozY());

    // }
    // @Test
    // void labelTest(){
    //     this.label ="e9";
    //     Square square=new Square(label);
    //     assertEquals(label,square.getLabel());
    // }

}